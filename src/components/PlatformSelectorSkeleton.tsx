import { Skeleton } from "@chakra-ui/react";

const PlatformSelectorSkeleton = () => {
  return (
    <Skeleton
      marginLeft={2}
      width="118px"
      height="40px"
      borderRadius={8}></Skeleton>
  );
};

export default PlatformSelectorSkeleton;
